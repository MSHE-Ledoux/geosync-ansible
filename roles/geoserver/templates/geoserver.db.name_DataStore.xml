<dataStore>
  <name>{{ geoserver.db.name }}</name>
  <connectionParameters>
    <host>localhost</host>
    <port>5432</port>
    <database>{{ geoserver.db.name }}</database>
    <user>{{ geoserver.db.user }}</user>
    <passwd>{{ geoserver.db.pass }}</passwd>
    <dbtype>postgis</dbtype>
  </connectionParameters>
</dataStore>
